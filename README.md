# Three ways of fibonacci

Just a little time capsule for myself or anyone else to calculate the Fibonacci number.

One uses the `yield` operator, one is `recursive` and the last one is a simple loop.
